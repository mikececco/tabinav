<div class="container mx-auto p-8 px-40">
  <h1 class="text-5xl font-bold py-8">Trending routes</h1>
  <div class="flex flex-wrap -mx-4">
  <% @routes.each do |route| %>
    <div class="w-full sm:w-1/2 md:w-1/2 xl:w-1/4 p-4 rounded-lg hover:scale-110 transition duration-1000 ease-in-out pb-8">
      <%= render "shared/new_card", route: route %>
    </div>
  <% end %>
  </div>
</div>

<%# <div class=" h-96 flex items-center">
    <div class=" h-full w-full">
      <%= render "shared/map" %>
